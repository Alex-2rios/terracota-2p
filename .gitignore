# Dependencias
node_modules/
**/node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Variables de entorno reales (los .example SÍ se versionan)
.env
.env.*
!.env.example

# Nativo / firmas
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.pem
*.mobileprovision
# Credenciales de la keystore de firma del APK.
firma.properties
/ios
/android
# `expo prebuild` genera estos dentro de movil/: se regeneran a partir de
# app.json, no se versionan. Las reglas de arriba están ancladas a la raíz.
movil/android/
movil/ios/

# Documentación: se mantiene en local, fuera del repositorio.
*.md

# Sistema y editores
.DS_Store
Thumbs.db
.metro-health-check*
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo

# Artefactos de compilación de la app móvil.
*.apk
*.aab
