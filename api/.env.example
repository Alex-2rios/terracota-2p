# Copia este archivo como `.env` sólo si ejecutas la API fuera de Docker.
# Con `docker compose up` las variables vienen del .env de la raíz del proyecto.

DATABASE_URL=postgresql://terracota_app:TerracotaLocal123!@localhost:5433/terracota
JWT_SECRET=cambia-esta-clave-por-al-menos-32-caracteres-aleatorios
ACCESS_TOKEN_MINUTES=480
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:5000,http://127.0.0.1:5000,http://localhost:8081,http://localhost:19006
LOGIN_MAX_INTENTOS=8
LOGIN_VENTANA_SEGUNDOS=300
